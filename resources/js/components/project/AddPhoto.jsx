import React from "react";

export const AddPhoto = ({ setPhotos }) => {
    return (
        <div className="text-center">
            <div className="btn btn-outline btn-primary">add</div>
        </div>
    );
};
